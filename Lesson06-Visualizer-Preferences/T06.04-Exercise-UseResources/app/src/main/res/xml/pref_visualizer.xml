<?xml version="1.0" encoding="utf-8"?>

<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Complete (2) Create a res->bools.xml file and create a boolean for this default value -->
    <!-- Complete (3) Update these values to be values from resource files and not hard coded -->

    <CheckBoxPreference
        android:defaultValue="@bool/pref_default_value"
        android:key="@string/pref_key_show_bass"
        android:summaryOff="@string/pref_summary_off_value"
        android:summaryOn="@string/pref_summary_on_value"
        android:title="@string/pref_title_value" />

</PreferenceScreen>
